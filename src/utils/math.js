const multiplication = (a, b)=>{
    return a * b;
}

const squareRoot = (a) =>{
    return Math.pow(a, 2);
}

const sinus = (number) =>{
    return Math.sin(number);
}

export {multiplication, squareRoot, sinus}
